## Setup

This recipe is documentation-first. Use whichever Reason 2 inference path you already have.

## Option 1 — Use the Cosmos Cookbook environment

If you already ran `just install`, you can run the docs site locally:

```bash
just serve-internal
```
